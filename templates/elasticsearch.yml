# This config is autogenerated by ansible.
# See templates/elasticsearch.yml in the charm folder.

cluster.name: {{ cluster_name }}
http.port: 9200
network.bind_host: ["_site_", "_local_"]
network.publish_host: {{ unit_private_address }}
path.data: /var/lib/elasticsearch
path.logs: /var/log/elasticsearch
{% if (relations.peer is defined) and relations.peer %}
discovery.zen.ping.unicast.hosts:
{% for reln in relations.peer %}
  - {{ reln['private-address'] }}
{% endfor %}
{% endif %}
